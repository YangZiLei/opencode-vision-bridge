# OS
.DS_Store
Thumbs.db
desktop.ini

# Logs & temp
*.log
*.tmp
node_modules/

# OpenCode local state (contains credentials — never commit)
opencode.json
opencode.jsonc
auth.json
.env
.env.*
!.env.example

# Editor
.idea/
.vscode/
*.swp

# Test/runtime artifacts
coverage/

# WorkBuddy local project state (agent memory/runtime — not for public repo)
.workbuddy/
