{
  "_comment": "textModels = fallback blacklist for text-only models. forceBridge = models whose metadata CLAIMS image support but actually fail on image input (they get bridged to the vision subagent regardless). Use wildcards. No trailing comma after the last item.",
  "textModels": [
    "*/deepseek*",
    "*/hy3*"
  ],
  "forceBridge": []
}